using UnityEngine;

/// <summary>
/// A custom attribute to make a property read-only in the Unity Inspector.
/// </summary>
public class ReadOnlyAttribute : PropertyAttribute
{
    // This attribute doesn't need any data, so the class body is empty.
}