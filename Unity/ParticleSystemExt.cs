using System.Collections.Generic;
using UnityEngine;

public static class ParticleSystemExt
{
    // public static int[] ParticleIndexesByLifeRemaining(this ParticleSystem.Particle[] particleArray)
    // {
    //     int l = particleArray.Length;
    //     Dictionary<int, float> particles = new Dictionary<int, float>(l);

    //     for (int i = 0; i < l; i++)
    //     {
    //         particles[i] = particleArray[i].remainingLifetime;
    //     }

    //     return particles.OrderBy(x => x.Value).Select(x => x.Key).ToArray();
    // }
}